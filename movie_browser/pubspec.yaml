name: movie_browser
description: "A movie browser using the OMDb Api"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  dio: ^5.9.1
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8
  

flutter:
  uses-material-design: true

  